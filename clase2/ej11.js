/*
    while:
        Esta estructura de repetición permite repetir un bloque de código mientras se cumpla la condición establecida.
        Esta estructura puede o no ejecutarse según la condición dada.

        while (condicion) {
            repetir
        }

        Esto lo podemos leer como: MIENTRAS se cumpla la CONDICION HACER
*/

// Imprimir en pantalla números del 0 al 10
var contador = 0;

while (contador <= 10) {
    console.log(contador);
    contador++;
}

// Imprimir en pantalla números del 0 al 10
var contador = 0;

while (contador <= 10) {
    console.log(contador);
    contador++;
}
