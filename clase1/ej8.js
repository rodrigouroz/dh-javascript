/*
    Array:

    Es una lista de cero o más elementos separados por comas. Su longitud se
    define por la cantidad de elementos que tiene. Un array soporta cualquier
    tipo de dato como elemento.
*/

// Lista vacía, no tiene elementos.
var listaVacia = [];

// Lista de notas musicales con 7 elementos string.
var notasMusicales = ['do', 're', 'mi', 'fa', 'sol', 'la', 'si'];

// Lista de notas de los parciales con 5 elementos number.
var notasParciales = [7, 8, 9, 6, 10];

// Lista de configuracion con 3 elementos boolean.
var configuracion = [true, true, false];

// Lista de elementos para una fiesta con valores múltiples
var fiesta = [ 40, 'mendoza 1432 4to B', true, false, ['coca', 'sprite', 'agua']];

console.log(notasMusicales);
console.log(notasParciales);
console.log(configuracion);
console.log(fiesta);
